<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             xmlns:dashboard="clr-namespace:Ambinity.Views.Screens.Dashboard"
             x:DataType="dashboard:DashboardDeviceViewModel"
             x:Class="Ambinity.Views.Screens.Dashboard.DashboardDeviceView">
    <Design.DataContext>
        <dashboard:DashboardDeviceViewModel/>
    </Design.DataContext>
    <Grid>
        <Button Command="{Binding DeviceClickedCommand}" Margin="10"  CornerRadius="7" Width="230" Height="270">
             
            <Grid RowDefinitions="*,auto,auto">
                <TextBlock Margin="10,30,0,0"
                           Text="{Binding Device.Name}"
                           FontSize="20" FontWeight="SemiBold">
                </TextBlock>
                <Button Grid.Row="1" Command="{Binding DeviceClickedCommand}"></Button>
            </Grid>
            
        </Button>
      
    </Grid>
</UserControl>
